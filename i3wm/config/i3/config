# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTY
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:Liberation Mono 8
#font pango:monospace 8
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 6

# The powerline font has a typo in the font name.
font pango:Literation Mono Nerd Font 8
# Before i3 v4.8, we used to recommend this one as the default:
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $up k
set $down j
set $left h
set $right l

# Use Mouse+Mod1 to drag floating windows to their wanted position
floating_modifier Mod1

# start a terminal
bindsym Mod1+Return exec "~/.local/bin/start-tmux-x.sh"
bindsym Mod1+Shift+Return exec "st -e tmux new"

# blank the screen
#bindsym --release Mod1+Control+Shift+s exec "xset dpms force off"

# En/disable the Yubikey (to avoid keystrokes if bumped).
bindsym Mod1+Control+Shift+y exec "xinput enable 'Yubico Yubikey 4 OTP+U2F+CCID'"
bindsym Mod1+Shift+y exec "xinput disable 'Yubico Yubikey 4 OTP+U2F+CCID'"

bindsym Mod1+Shift+x exec "~/.nix-profile/bin/xkill"

# Suspend
bindsym Mod1+Control+Shift+u exec "systemctl suspend"

# Fix bluetooth packet drops on headset.
bindsym Mod1+Shift+b exec "pactl suspend-sink 1 && pactl suspend-sink 0"

# Change brightness.
bindsym Mod1+Shift+Control+b exec "~/.local/bin/setbacklight.sh"

# Enter a password (by typing it in automatically).
bindsym Mod1+Shift+a exec "~/.local/bin/passmenu --type"

# Copy a password to the clipboard.
bindsym Mod1+Control+Shift+a exec "~/.local/bin/passmenu"

# kill focused window
bindsym Mod1+Shift+q kill

# start dmenu (a program launcher)
bindsym Mod1+d exec dmenu_run
bindsym Mod1+Shift+d exec --no-startup-id i3-dmenu-desktop
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym Mod1+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym Mod1+h focus left
bindsym Mod1+j focus down
bindsym Mod1+k focus up
bindsym Mod1+l focus right

# alternatively, you can use the cursor keys:
#bindsym Mod1+Left focus left
#bindsym Mod1+Down focus down
#bindsym Mod1+Up focus up
#bindsym Mod1+Right focus right

# move focused window
bindsym Mod1+Shift+h move left
bindsym Mod1+Shift+j move down
bindsym Mod1+Shift+k move up
bindsym Mod1+Shift+l move right

# alternatively, you can use the cursor keys:
#bindsym Mod1+Shift+Left move left
#bindsym Mod1+Shift+Down move down
#bindsym Mod1+Shift+Up move up
#bindsym Mod1+Shift+Right move right

# split in horizontal orientation
bindsym Mod1+o split h

# split in vertical orientation
bindsym Mod1+v split v

# enter fullscreen mode for the focused container
bindsym Mod1+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym Mod1+s layout stacking
bindsym Mod1+w layout tabbed
bindsym Mod1+e layout toggle split

# toggle tiling / floating
bindsym Mod1+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym Mod1+space focus mode_toggle

# focus the parent container
bindsym Mod1+a focus parent

# focus the child container
bindsym Mod1+c focus child

# move the currently focused window to the scratchpad
bindsym Mod1+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym Mod1+minus scratchpad show

bindsym Mod1+equal [class="^Spotify$"] scratchpad show

# Switch/move container to the next/previous workspace
bindsym Mod1+n workspace next
bindsym Mod1+p workspace prev
bindsym Mod1+Shift+n move container to workspace next
bindsym Mod1+Shift+p move container to workspace prev


# reload the configuration file
bindsym Mod1+Control+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym Mod1+Control+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym Mod1+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit & sleep 10 && loginctl terminate-session $XDG_SESSION_ID'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 5 ppt
        bindsym j resize grow height 10 px or 5 ppt
        bindsym k resize shrink height 10 px or 5 ppt
        bindsym l resize grow width 10 px or 5 ppt

	bindsym Mod1+h resize shrink width 1 px or 1 ppt
	bindsym Mod1+j resize grow height 1 px or 1 ppt
	bindsym Mod1+k resize shrink height 1 px or 1 ppt
	bindsym Mod1+l resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 10 px or 10 ppt
        #bindsym Down resize grow height 10 px or 10 ppt
        #bindsym Up resize shrink height 10 px or 10 ppt
        #bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym Mod1+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
	id bar-top
	tray_output none
	workspace_buttons no
	binding_mode_indicator no
	status_command i3pystatus -c ~/.config/i3pystatus/top.py
	colors {
		background #000000
		statusline #ffffff
		separator  #666666
		
		focused_workspace  #7e3fa0 #6c2593 #ffffff
		active_workspace   #68627e #4f4865 #ffffff
		inactive_workspace #443344 #332233 #998899
		urgent_workspace   #c0458b #b02574 #ffffff
		binding_mode       #c0458b #b02574 #ffffff
	}
}
bar {
	position bottom
	id bar-bottom
	status_command i3pystatus -c ~/.config/i3pystatus/bottom.py
	colors {
		background #000000
		statusline #ffffff
		separator  #666666
		
		focused_workspace  #7e3fa0 #6c2593 #ffffff
		active_workspace   #68627e #4f4865 #ffffff
		inactive_workspace #443344 #332233 #998899
		urgent_workspace   #c0458b #b02574 #ffffff
		binding_mode       #c0458b #b02574 #ffffff
	}
}

# Lock the screen
bindsym Mod1+Control+Shift+l exec "sudo -K; i3lock -c 202020 -e"

# Input i3 commands
bindsym Mod1+i exec "i3-input"

# Rename the current workspace
bindsym Mod1+Shift+r exec i3-input -F 'rename workspace to "%s"' -P 'New name: '

# Open pavucontrol
bindsym Mod1+Control+Shift+p exec "pavucontrol"

# Open alsamixer in a terminal.
#bindsym Mod1+Control+Shift+a exec "st -e alsamixer -c 0"

# Open htop in a terminal.
bindsym Mod1+Control+Shift+h exec "st -e htop"

# Take a screenshot of the window
bindsym --release Mod1+Shift+s exec "scrot -b -u -e 'pngcrush -brute -l 9 -reduce $f $f.crush.png && mv $f.crush.png $f' $(mktemp $(date '+/home/iamthememory/screenshots/screenshot-%Y-%m-%d-%H:%M:%S.%N%z.XXXX.png'))"

# Take a screenshot of the screen
bindsym --release Mod1+Control+s exec "scrot -b -e 'pngcrush -brute -l 9 -reduce $f $f.crush.png && mv $f.crush.png $f' $(mktemp $(date '+/home/iamthememory/screenshots/screenshot-%Y-%m-%d-%H:%M:%S.%N%z.XXXX.png'))"

# Take a screenshot of the specified area
bindsym --release Mod1+Control+Shift+s exec "scrot -b -s -e 'pngcrush -brute -l 9 -reduce $f $f.crush.png && mv $f.crush.png $f' $(mktemp $(date '+/home/iamthememory/screenshots/screenshot-%Y-%m-%d-%H:%M:%S.%N%z.XXXX.png'))"

# Spam a hundred clicks at the current mouse position
#bindsym Mod1+Shift+i exec "eval $(xdotool getmouselocation --shell); for x in {1..100}; do xdotool mousemove $X $Y click 1; sleep 0.15; done"

# Spam 500 right clicks at the current mouse position
#bindsym --release Mod1+Control+Shift+i exec "sleep 0.5; eval $(xdotool getmouselocation --shell); for x in {1..500}; do xdotool mousemove $X $Y click 3; sleep 0.2; done"

# Spam a bunch of a's
#bindsym --release Mod1+Shift+a exec "sleep 0.5; for x in {1..50}; do xdotool key a; sleep 0.1; done"

# Turn the touchpad off
bindsym Mod1+Shift+t exec "xinput disable 'pointer:SynPS/2 Synaptics TouchPad'"

# Turn the touchpad on
bindsym Mod1+Control+Shift+t exec "xinput enable 'pointer:SynPS/2 Synaptics TouchPad'"

# Change the volume.
bindsym XF86AudioRaiseVolume exec "pamixer --increase 5; killall -USR1 i3status"
bindsym XF86AudioLowerVolume exec "pamixer --decrease 5; killall -USR1 i3status"
bindsym XF86AudioMute exec "~/.local/bin/toggle-mute.sh"

#bindsym XF86AudioPlay exec "mpc toggle"
#bindsym XF86AudioPause exec "mpc toggle"
#bindsym Mod1+Control+n exec "mpc next"
#bindsym XF86AudioNext exec "mpc next"
#bindsym Mod1+Control+p exec "mpc prev"
#bindsym XF86AudioPrev exec "mpc prev"

bindsym XF86AudioPlay  exec "playerctl --player=spotify play-pause"
bindsym XF86AudioPause exec "playerctl --player=spotify play-pause"
bindsym Mod1+Control+n exec "playerctl --player=spotify next"
bindsym XF86AudioNext  exec "playerctl --player=spotify next"
bindsym Mod1+Control+p exec "playerctl --player=spotify previous"
bindsym XF86AudioPrev  exec "playerctl --player=spotify previous"

# Open nautilus.
bindsym Mod1+Control+Shift+n exec "nautilus"

# Open chromium.
bindsym Mod1+Control+c exec "chromium-browser"
bindsym Mod1+Control+Shift+c exec "chromium-browser --incognito"

# Swap caps lock and escape.
bindsym Mod1+Control+Shift+g exec "xmodmap ~/.config/speedswapper"

# Open FFXIV.
bindsym Mod1+Control+Shift+f exec "playonlinux --run 'FINAL FANTASY XIV - A Realm Reborn'"

# Open Minecraft with FTB.
bindsym Mod1+Control+Shift+m exec "ftb-launch.sh"


# Allow special keys to passthrough.
mode "passthrough" {
        bindsym Mod1+t mode "default"
}
bindsym Mod1+t mode "passthrough"


# The default colors.
# class                 border  backgr. text    indicator child_border
#client.focused          #4c7899 #285577 #ffffff #2e9ef4   #285577
#client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
#client.unfocused        #333333 #222222 #888888 #292d2e   #222222
#client.urgent           #2f343a #900000 #ffffff #900000   #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
#client.background       #ffffff

# class                 border  backgr. text    indicator child_border
#client.focused          #996699 #664477 #ffffff #f49ef4   #664477
#client.focused_inactive #332233 #5f556a #ffffff #484e50   #5f676a
#client.unfocused        #333333 #222222 #888888 #292d2e   #222222
#client.urgent           #2f343a #900000 #ffffff #900000   #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
#client.background       #ffffff

# <http://paletton.com/#uid=54H0u1kperCfyB2k-u6snmHvThUkperCfyB2k-u6snmHvThUkaxit5ash85mVbXc9jwbs>
# class                 border  backgr. text    indicator child_border
client.focused          #7e3fa0 #6c2593 #ffffff #9c65ba   #6c2593
client.focused_inactive #68627e #4f4865 #ffffff #8a859b   #4f4865
client.unfocused        #443344 #332233 #998899 #3e2d3e   #332233
client.urgent           #c0458b #b02574 #ffffff #d66ea9   #b02574
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff


# Workspaces.

workspace_layout tabbed

bindsym Mod1+1 workspace number 1:disc
bindsym Mod1+Shift+1 move container to workspace number 1:disc
assign [class="^discord$"] 1:disc

bindsym Mod1+2 workspace number 2:sys
bindsym Mod1+Shift+2 move container to workspace number 2:sys
assign [class="^deluge$"] 2:sys

bindsym Mod1+3 workspace number 3:mail
bindsym Mod1+Shift+3 move container to workspace number 3:mail
assign [class="^Thunderbird$"] 3:mail

bindsym Mod1+4 workspace number 4:ffdoc
bindsym Mod1+Shift+4 move container to workspace number 4:ffdoc

bindsym Mod1+5 workspace number 5:ffxiv
bindsym Mod1+Shift+5 move container to workspace number 5:ffxiv
assign [instance="^ffxivlauncher\.exe$"] 5:ffxiv
assign [instance="^ffxiv\.exe$"] 5:ffxiv
assign [instance="^ffxiv_dx11\.exe$"] 5:ffxiv
assign [instance="^ffxivboot\.exe$"] 5:ffxiv
for_window [instance="^ffxiv\.exe$"] border none

bindsym Mod1+6 workspace number 6
bindsym Mod1+Shift+6 move container to workspace number 6

bindsym Mod1+7 workspace number 7
bindsym Mod1+Shift+7 move container to workspace number 7

bindsym Mod1+8 workspace number 8:slack
bindsym Mod1+Shift+8 move container to workspace number 8:slack
assign [class="^Slack"] 8:slack

bindsym Mod1+9 workspace number 9:misc
bindsym Mod1+Shift+9 move container to workspace number 9:misc

bindsym Mod1+0 workspace number 10:steam
bindsym Mod1+Shift+0 move container to workspace number 10:steam
assign [class="^Steam$"] 10:steam

for_window [class="^Spotify$"] move scratchpad
for_window [class="^Skype$"]   move scratchpad
for_window [class="^Keybase$"] move scratchpad

#exec --no-startup-id redshift-gtk
exec dunst
exec discord.sh
#exec --no-startup-id run_keybase
exec --no-startup-id mpd
exec steam
exec thunderbird
#exec --no-startup-id "gksudo aa-notify -p -f /var/log/audit/audit.log --display $DISPLAY"
#exec --no-startup-id /usr/lib64/libexec/kdeconnectd
exec spotify
exec slack
exec xcompmgr -c -n
exec kbfsfuse /keybase
exec keybase-gui
