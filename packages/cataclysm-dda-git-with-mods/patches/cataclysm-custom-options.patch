diff --git a/data/core/external_options.json b/data/core/external_options.json
index 9e17b54f3ed3..c6eae79d7de3 100644
--- a/data/core/external_options.json
+++ b/data/core/external_options.json
@@ -520,5 +520,180 @@
     "//": "The maximum number of overmaps that an intersection can deviate from its gridded position. Cannot be greater than or equal to row / 2 or column / 2, may cause bugs for > row / 4, column / 4.",
     "stype": "int",
     "value": 2
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "UNSTABLE_TRAIT_MUTATION_TIME",
+    "//": "The average hours between mutations with the unstable trait.",
+    "stype": "int",
+    "value": 48
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "CHAOTIC_TRAIT_MUTATION_TIME",
+    "//": "The average hours between mutations with the chaotic trait.",
+    "stype": "int",
+    "value": 12
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "BASE_MANA_REGEN_TIME",
+    "//": "How many hours it should take to regenerate all mana at base mana regen.",
+    "stype": "float",
+    "value": 8.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "BASE_MANA_REGEN_TIME",
+    "//": "How many hours it should take to regenerate all mana at base mana regen.",
+    "stype": "float",
+    "value": 8.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "CROP_GROWTH_TIME_SCALER",
+    "//": "A scaler for how long crops take to grow. 0.5 will make crops take half as much time to grow.",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "CROP_GROWTH_FORCE_SEEDS",
+    "//": "Whether to force crops to spawn seeds when harvest.",
+    "stype": "bool",
+    "value": false
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "CROP_YIELD_MULTIPLIER",
+    "//": "How to scale crop yields. 2.0 will double crop yields.",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "SHELF_LIFE_MULTIPLIER",
+    "//": "How much longer than normal foods will last. 2.0 will cause foods to last twice as long.",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "COMESTIBLES_GO_BAD",
+    "//": "Whether or not comestibles spoil.",
+    "stype": "bool",
+    "value": true
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "BUTCHERY_TIME_MULTIPLIER",
+    "//": "How much longer than normal butchering will take. 0.5 will cause butchering to take half as long..",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "BREW_TIME_MULTIPLIER",
+    "//": "How much longer than normal brewing will take. 0.5 will cause brewing to take half as long..",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "COMPOST_TIME_MULTIPLIER",
+    "//": "How much longer than normal composting will take. 0.5 will cause composting to take half as long..",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "RECIPE_TIME_MULTIPLIER",
+    "//": "How much longer than normal crafting will take. 0.5 will cause crafting to take half as long..",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "VEHICLE_WORK_TIME_MULTIPLIER",
+    "//": "How much longer than normal working on vehicles will take. 0.5 will cause working on vehicles to take half as long..",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "FOCUS_EQUILIBRIUM_PENALTY",
+    "//": "How much of a penalty to give to the focus equilibrium point while reading. 40 will aim focus towards 100 - 40 = 60.",
+    "stype": "int",
+    "value": 50
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "TRADER_AVOID_ITEMS",
+    "//": "Whether traders will respect the TRADER_AVOID flag and refuse to buy items marked with it.",
+    "stype": "bool",
+    "value": true
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "TRADER_AVOID_FILTHY",
+    "//": "Whether traders will refuse to buy filthy items or not.",
+    "stype": "bool",
+    "value": true
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "TRADER_RESPECT_WHITELIST",
+    "//": "Whether traders will respect their whitelists.",
+    "stype": "bool",
+    "value": true
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "TRADER_RESPECT_BLACKLIST",
+    "//": "Whether traders will respect their blacklists.",
+    "stype": "bool",
+    "value": true
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "DONT_AUTOLEARN_BOOK_RECIPES",
+    "//": "Whether to prevent autolearning recipes which can be learned via a book.",
+    "stype": "bool",
+    "value": false
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "DONT_AUTOLEARN_DISASSEMBLY_RECIPES",
+    "//": "Whether to prevent autolearning recipes which can be learned via disassembly.",
+    "stype": "bool",
+    "value": false
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "REQUIRE_THEORETICAL_KNOWLEDGE_TO_LEARN",
+    "//": "Learning practical skill will no longer increase your theoretical skill, so you need to learn from books to get any skill.",
+    "stype": "bool",
+    "value": false
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "EREADER_DRAIN_INTERVAL_MULTIPLIER",
+    "//": "A multiplier for how often reading ebooks should drain power, where 2.0 means it will take twice as long between power usages, and use half the power.",
+    "stype": "float",
+    "value": 1.0
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "UNCANNY_DODGE_TRAIT_STAMINA_COST",
+    "//": "How much stamina using uncanny dodge via mutations should cost. At the default player stamina burn rate, normal dodge attempts cost 90 stamina at dodge level 0, 67.5 at level 5, and 45 at level 10.",
+    "stype": "int",
+    "value": 300
+  },
+  {
+    "type": "EXTERNAL_OPTION",
+    "name": "DISABLE_VEHICLE_CRAMPING",
+    "//": "Whether to disable being cramped in a vehicle.",
+    "stype": "bool",
+    "value": false
   }
 ]
diff --git a/src/activity_actor.cpp b/src/activity_actor.cpp
index ba611058b0ae..dfd3a265dbb7 100644
--- a/src/activity_actor.cpp
+++ b/src/activity_actor.cpp
@@ -2174,7 +2174,7 @@ void read_activity_actor::do_turn( player_activity &act, Character &who )
         return;
     }
 
-    if( using_ereader && calendar::once_every( 5_minutes ) ) {
+    if( using_ereader && calendar::once_every( 5_minutes * ::get_option<float>( "EREADER_DRAIN_INTERVAL_MULTIPLIER" ) ) ) {
         /** Expected battery life while reading with common ereaders:
                 integrated_ar       - implant                                         = potentially infinite
                 ar_glasses_advanced - max 300 charges from disposable light battery   = 25 hours of reading
diff --git a/src/butchery.cpp b/src/butchery.cpp
index 796fe183f260..5007068aa89d 100644
--- a/src/butchery.cpp
+++ b/src/butchery.cpp
@@ -35,6 +35,7 @@
 #include "mtype.h"
 #include "npc.h"
 #include "npc_opinion.h"
+#include "options.h"
 #include "output.h"
 #include "player_activity.h"
 #include "pocket_type.h"
@@ -490,6 +491,7 @@ int butcher_time_to_cut( Character &you, const item &corpse_item, const butcher_
     }
 
     time_to_cut *= ( 1.0f - ( get_player_character().get_num_crafting_helpers( 3 ) / 10.0f ) );
+    time_to_cut *= ::get_option<float>( "BUTCHERY_TIME_MULTIPLIER" );
     return time_to_cut;
 }
 
diff --git a/src/character.cpp b/src/character.cpp
index 5accf2c71012..b37b24b0c3ef 100644
--- a/src/character.cpp
+++ b/src/character.cpp
@@ -379,6 +379,7 @@ static const trait_id trait_VISCOUS( "VISCOUS" );
 static const std::set<material_id> ferric = { material_iron, material_steel, material_budget_steel, material_ch_steel, material_hc_steel, material_lc_steel, material_mc_steel, material_qt_steel };
 
 static const std::string player_base_stamina_burn_rate( "PLAYER_BASE_STAMINA_BURN_RATE" );
+static const std::string uncanny_dodge_trait_stamina_cost( "UNCANNY_DODGE_TRAIT_STAMINA_COST" );
 static const std::string type_hair_color( "hair_color" );
 static const std::string type_hair_style( "hair_style" );
 static const std::string type_skin_tone( "skin_tone" );
@@ -1923,14 +1924,15 @@ bool Character::uncanny_dodge()
     bool seen = get_player_view().sees( here, *this );
 
     const units::energy trigger_cost = bio_uncanny_dodge->power_trigger;
+    const int stamina_cost = get_option<int>( player_base_stamina_burn_rate );
 
     const bool can_dodge_bio = get_power_level() >= trigger_cost &&
                                has_active_bionic( bio_uncanny_dodge );
-    const bool can_dodge_stam = get_stamina() >= 300 && ( has_trait_flag( json_flag_UNCANNY_DODGE ) ||
+    const bool can_dodge_stam = get_stamina() >= stamina_cost && ( has_trait_flag( json_flag_UNCANNY_DODGE ) ||
                                 has_effect_with_flag( json_flag_UNCANNY_DODGE ) );
     const bool can_dodge_both = get_power_level() >= ( trigger_cost / 2 ) &&
                                 has_active_bionic( bio_uncanny_dodge ) &&
-                                get_stamina() >= 150 && has_trait_flag( json_flag_UNCANNY_DODGE );
+                                get_stamina() >= stamina_cost / 2 && has_trait_flag( json_flag_UNCANNY_DODGE );
 
     if( !( can_dodge_bio || can_dodge_stam || can_dodge_both ) ) {
         return false;
@@ -1963,11 +1965,11 @@ bool Character::uncanny_dodge()
 
         if( can_dodge_both ) {
             mod_power_level( -trigger_cost / 2 );
-            burn_energy_legs( -150 );
+            burn_energy_legs( -stamina_cost / 2 );
         } else if( can_dodge_bio ) {
             mod_power_level( -trigger_cost );
         } else if( can_dodge_stam ) {
-            burn_energy_legs( -300 );
+            burn_energy_legs( -stamina_cost );
         }
         return true;
     }
@@ -2556,6 +2558,16 @@ bool Character::practice( const skill_id &id, int amount, int cap, bool suppress
         return false;
     }
 
+    if( ::get_option<bool>( "REQUIRE_THEORETICAL_KNOWLEDGE_TO_LEARN" ) ) {
+        int theoretical_level = level.knowledgeLevel() * 100 + std::max( 0, level.knowledgeExperience() );
+        int practical_level = level.level() * 100 + std::max( 0, level.exercise() );
+        if( practical_level >= theoretical_level ) {
+            // Don't practice if we need more theoretical knowledge.
+            return false;
+        }
+    }
+
+
     // Your ability to "catch up" skill experience to knowledge is mostly a function of intelligence,
     // but perception also plays a role, representing both memory/attentiveness and catching on to how
     // the two apply to each other.
diff --git a/src/character_health.cpp b/src/character_health.cpp
index a40b42d8e9ae..6d1e0d4d47a0 100644
--- a/src/character_health.cpp
+++ b/src/character_health.cpp
@@ -524,7 +524,7 @@ int Character::calc_focus_equilibrium( bool ignore_pain ) const
             // apply a penalty when we're actually learning something
             const SkillLevel &skill_level = get_skill_level_object( bt->skill );
             if( skill_level.can_train() && skill_level < bt->level ) {
-                focus_equilibrium -= 50;
+                focus_equilibrium -= ::get_option<int>( "FOCUS_EQUILIBRIUM_READING_PENALTY" );
             }
         }
     }
diff --git a/src/creature.cpp b/src/creature.cpp
index 655c65c7b01d..1df43d66afe2 100644
--- a/src/creature.cpp
+++ b/src/creature.cpp
@@ -234,6 +234,10 @@ void Creature::setpos( const tripoint_abs_ms &p, bool check_gravity/* = true*/ )
 
 bool Creature::will_be_cramped_in_vehicle_tile( map &here, const tripoint_abs_ms &loc ) const
 {
+    if( ::get_option<bool>( "DISABLE_VEHICLE_CRAMPING" ) ) {
+        return false;
+    }
+
     const optional_vpart_position vp_there = here.veh_at( loc );
     if( !vp_there ) {
         return false;
diff --git a/src/iexamine.cpp b/src/iexamine.cpp
index ca2a5bdc372a..035b5c2ef2ec 100644
--- a/src/iexamine.cpp
+++ b/src/iexamine.cpp
@@ -2831,11 +2831,16 @@ void iexamine::harvest_plant( Character &you, const tripoint_bub_ms &examp, bool
 
             int skillLevel = round( you.get_skill_level( skill_survival ) );
             ///\EFFECT_SURVIVAL increases number of plants harvested from a seed
-            int plant_count = rng( skillLevel / 2, skillLevel );
+            float yield_multiplier = ::get_option<float>( "CROP_YIELD_MULTIPLIER" );
+            int plant_count = rng(
+                    static_cast<int>( skillLevel * yield_multiplier / 2.0 ),
+                    static_cast<int>( skillLevel * yield_multiplier ) );
             const auto &fp = here.furn( examp )->plant;
             plant_count *= fp->harvest_multiplier;
-            plant_count = std::min( std::max( plant_count, 1 ), 12 );
-            int seedCount = std::max( 1, rng( plant_count / 4, plant_count / 2 ) );
+            plant_count = std::min(
+                    std::max( plant_count, 1 ),
+                    static_cast<int>( 12.0 * yield_multiplier ) );
+            int seedCount = std::max( 1, rng( plant_count / 4, std::max( plant_count / 2, 2 ) ) );
             for( item &i : get_harvest_items( type, plant_count, seedCount, true ) ) {
                 if( from_activity ) {
                     i.set_var( "activity_var", you.name );
diff --git a/src/item_factory.cpp b/src/item_factory.cpp
index 0888c06faf09..8decaf98b3ae 100644
--- a/src/item_factory.cpp
+++ b/src/item_factory.cpp
@@ -544,6 +544,20 @@ void Item_factory::finalize_pre( itype &obj )
         }
     }
 
+    if( obj.brewable ) {
+        obj.brewable->time *= ::get_option<float>( "BREW_TIME_MULTIPLIER" );
+    }
+
+    if( obj.compostable ) {
+        obj.compostable->time *= ::get_option<float>( "COMPOST_TIME_MULTIPLIER" );
+    }
+
+    if( obj.seed ) {
+        if( ::get_option<bool>( "CROP_GROWTH_FORCE_SEEDS" ) ) {
+            obj.seed->spawn_seeds = true;
+        }
+    }
+
     if( obj.bionic ) {
         obj.bionic->id = bionic_id( obj.id.str() );
     }
@@ -823,6 +837,12 @@ void Item_factory::finalize_pre( itype &obj )
                 }
             }
         }
+
+        if ( !get_option<bool>( "COMESTIBLES_GO_BAD" ) ) {
+            obj.comestible->spoils = time_duration::from_turns(0);
+        } else {
+            obj.comestible->spoils *= get_option<float>( "SHELF_LIFE_MULTIPLIER" );
+        }
     }
 
     // recurse into subtypes, adding self as substitution for all items in the subtypes chain
diff --git a/src/magic.cpp b/src/magic.cpp
index 9dd39aab065a..9b5b73b230cf 100644
--- a/src/magic.cpp
+++ b/src/magic.cpp
@@ -2560,7 +2560,8 @@ int known_magic::max_mana( const Character &guy ) const
 void known_magic::update_mana( const Character &guy, float turns )
 {
     // mana should replenish in 8 hours.
-    const double full_replenish = to_turns<double>( 8_hours );
+    const double full_replenish =
+        to_turns<double>( 1_hours ) * ::get_option<float>( "BASE_MANA_REGEN_TIME" );
     const double ratio = turns / full_replenish;
     mod_mana( guy, std::floor( ratio * std::max( 0.0,
                                guy.calculate_by_enchantment( static_cast<double>( max_mana(
diff --git a/src/map.cpp b/src/map.cpp
index de8eca5b31eb..2c4538d2272d 100644
--- a/src/map.cpp
+++ b/src/map.cpp
@@ -9184,7 +9184,7 @@ void map::grow_plant( const tripoint_bub_ms &p )
             target_stage = pair.first;
         } // Don't break the loop for the case where time has been rewound.
         // Advance to the time of the next stage for the next iteration.
-        time_to_grow_to_this_stage += pair.second;
+        time_to_grow_to_this_stage += pair.second * ::get_option<float>( "CROP_GROWTH_TIME_SCALER" );
     }
 
     const auto check_flag = []( const std::string & to_check ) {
diff --git a/src/npc.cpp b/src/npc.cpp
index 7fab8542ebfa..507025c0b081 100644
--- a/src/npc.cpp
+++ b/src/npc.cpp
@@ -2118,15 +2118,16 @@ ret_val<void> npc::wants_to_buy( const item &it, int at_price ) const
     }
 
 
-    if( it.has_flag( flag_TRADER_AVOID ) || it.has_var( VAR_TRADE_IGNORE ) ) {
+    if( ::get_option<bool>( "TRADER_AVOID_ITEMS" ) &&
+            ( it.has_flag( flag_TRADER_AVOID ) || it.has_var( VAR_TRADE_IGNORE ) ) ) {
         return ret_val<void>::make_failure( _( "Will never buy this" ) );
     }
 
-    if( it.is_filthy() ) {
+    if( ::get_option<bool>( "TRADER_AVOID_FILTHY" ) && it.is_filthy() ) {
         return ret_val<void>::make_failure( _( "Will not buy filthy items" ) );
     }
 
-    if( myclass->has_whitelist() ) {
+    if( ::get_option<bool>( "TRADER_RESPECT_WHITELIST" ) && myclass->has_whitelist() ) {
         const shopkeeper_whitelist &wl = myclass->get_shopkeeper_whitelist();
         icg_entry const *wl_icg = myclass->get_shopkeeper_whitelist().matches( it, *this );
         if( wl_icg != nullptr ) {
@@ -2137,7 +2138,7 @@ ret_val<void> npc::wants_to_buy( const item &it, int at_price ) const
     }
 
     icg_entry const *bl = myclass->get_shopkeeper_blacklist().matches( it, *this );
-    if( bl != nullptr ) {
+    if( ::get_option<bool>( "TRADER_RESPECT_BLACKLIST" ) && bl != nullptr ) {
         return ret_val<void>::make_failure( bl->message.translated() );
     }
 
@@ -4246,5 +4247,3 @@ std::unique_ptr<talker> get_talker_for( npc *guy )
 {
     return std::make_unique<talker_npc>( guy );
 }
-
-
diff --git a/src/recipe.cpp b/src/recipe.cpp
index e3f6884bef16..d8eb0da4e074 100644
--- a/src/recipe.cpp
+++ b/src/recipe.cpp
@@ -40,6 +40,7 @@
 #include "mapgendata.h"
 #include "math_defines.h"
 #include "npc.h"
+#include "options.h"
 #include "output.h"
 #include "proficiency.h"
 #include "recipe_dictionary.h"
@@ -811,6 +812,15 @@ void recipe::finalize()
         check_blueprint_requirements();
     }
 
+    float recipe_time_multiplier = ::get_option<float>( "RECIPE_TIME_MULTIPLIER" );
+    if( has_steps() ) {
+        for( recipe_step &s : steps_ ) {
+            s.time *= recipe_time_multiplier;
+        }
+    } else {
+        time *= recipe_time_multiplier;
+    }
+
     if( !blueprint.is_empty() ) {
         incorporate_build_reqs();
     } else if( has_steps() ) {
@@ -922,6 +932,13 @@ void recipe::finalize()
         }
     }
 
+    if( ( ::get_option<bool>( "DONT_AUTOLEARN_BOOK_RECIPES" ) && !booksets.empty() ) ||
+            ( ::get_option<bool>( "DONT_AUTOLEARN_DISASSEMBLY_RECIPES" ) && !learn_by_disassembly.empty() ) ) {
+        // Prevent autolearning, since there's a way to learn it in the world.
+        autolearn = false;
+        autolearn_requirements.clear();
+    }
+
     // ensure result name is always in front of the name for searching in crafting menu
     if( !name_.empty() && !is_practice() && !is_nested() && result_ ) {
         name_ = translation::to_translation( string_format( name_.translated(),
diff --git a/src/skill.cpp b/src/skill.cpp
index 61afded790c8..61691361401e 100644
--- a/src/skill.cpp
+++ b/src/skill.cpp
@@ -395,7 +395,11 @@ void SkillLevel::train( int amount, float catchup_modifier, float knowledge_modi
         return;
     }
     _rustAccumulator -= catchup_amount;
-    _knowledgeExperience += knowledge_amount;
+
+    if( !::get_option<bool>( "REQUIRE_THEORETICAL_KNOWLEDGE_TO_LEARN" ) ) {
+        // Avoid raising theoretical knowledge outside of book learning.
+        _knowledgeExperience += knowledge_amount;
+    }
 
     on_exercise_change( allow_multilevel );
     practice();
diff --git a/src/suffer.cpp b/src/suffer.cpp
index aece4f6ef2bb..8a6ea8749c9c 100644
--- a/src/suffer.cpp
+++ b/src/suffer.cpp
@@ -1124,9 +1124,9 @@ void suffer::from_other_mutations( Character &you )
     }
 
     bool should_mutate = you.has_trait( trait_UNSTABLE ) && !you.has_trait( trait_CHAOTIC_BAD ) &&
-                         one_turn_in( 48_hours );
+                         one_turn_in( 1_hours * ::get_option<int>( "UNSTABLE_TRAIT_MUTATION_TIME" ) );
     should_mutate |= ( you.has_trait( trait_CHAOTIC ) || you.has_trait( trait_CHAOTIC_BAD ) ) &&
-                     one_turn_in( 12_hours );
+                     one_turn_in( 1_hours * ::get_option<int>( "CHAOTIC_TRAIT_MUTATION_TIME" ) );
     if( should_mutate ) {
         you.mutate();
     }
diff --git a/src/veh_type.cpp b/src/veh_type.cpp
index 51769e5d757e..74b67b6e192a 100644
--- a/src/veh_type.cpp
+++ b/src/veh_type.cpp
@@ -33,6 +33,7 @@
 #include "json.h"
 #include "magic_enchantment.h"
 #include "map.h"
+#include "options.h"
 #include "output.h"
 #include "pocket_type.h"
 #include "requirements.h"
@@ -737,6 +738,11 @@ void vpart_info::finalize()
     requirement_data::save_requirement( ins, ins_id );
     install_reqs.emplace_back( ins_id, 1 );
 
+    float work_time_multiplier = ::get_option<float>( "VEHICLE_WORK_TIME_MULTIPLIER" );
+    install_moves *= work_time_multiplier;
+    removal_moves *= work_time_multiplier;
+    repair_moves *= work_time_multiplier;
+
     if( removal_moves < 0_seconds ) {
         removal_moves = install_moves / 2;
     }
