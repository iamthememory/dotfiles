#!/bin/sh

# Source environment variables.


# Read our directory variables.
if [ -d ~/.env.d ]
then
  for file in ~/.env.d/*
  do
    [ -f "$file" ] && [ -r "$file" ] && . "$file"
  done
fi
unset file

# Read local overrides.
if [ -f ~/.env.local ] && [ -r ~/.env.local ]
then
  . ~/.env.local
fi
